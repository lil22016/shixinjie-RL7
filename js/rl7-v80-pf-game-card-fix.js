/* RL7 V82 — V80C DOM repair disabled.
V80C's MutationObserver + characterData + repeated DOM replacement caused a self-triggering mutation loop.
This file is intentionally inert. */
(function(){ 'use strict'; })();
